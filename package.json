{
  "name": "not-alone-rest",
  "version": "1.0.0",
  "description": "RESTful web service",
  "main": "app.js",
  "scripts": {
    "start": "DEBUG=* node app.js",
    "subcribe": "nodemon src/routes/arduino/mqtt-subcriber.js",
    "cobuild": "cd src/aqi-calculation/co-calculation && node-gyp configure && node-gyp build",
    "no2build": "cd src/aqi-calculation/no2-calculation && node-gyp configure && node-gyp build",
    "o3build": "cd src/aqi-calculation/o3-calculation && node-gyp configure && node-gyp build",
    "pm10build": "cd src/aqi-calculation/pm10-calculation && node-gyp configure && node-gyp build",
    "pm25build": "cd src/aqi-calculation/pm25-calculation && node-gyp configure && node-gyp build",
    "so2build": "cd src/aqi-calculation/so2-calculation && node-gyp configure && node-gyp build"
  },
  "author": "joebui",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^1.0.2",
    "body-parser": "^1.17.1",
    "cookie-parser": "^1.4.3",
    "cors": "^2.8.3",
    "express": "^4.15.2",
    "express-winston": "^2.4.0",
    "googleapis": "^23.0.0",
    "helmet": "^3.6.1",
    "json-query": "^2.2.2",
    "jsonwebtoken": "^7.3.0",
    "mongoose": "^4.9.3",
    "mqtt": "^2.8.0",
    "nan": "^2.6.2",
    "pubnub": "^4.10.0",
    "redis": "^2.7.1",
    "socket.io": "^1.7.3",
    "winston": "^2.3.1"
  },
  "devDependencies": {}
}
